/* ═══════════════════════════════════════════
   style.css — fonteboa
   Variáveis CSS no :root = um único ponto
   de controle para cores, fontes e espaços.
   ═══════════════════════════════════════════ */

@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@900&family=Lora:ital,wght@0,400;0,500;1,400&display=swap');

/* ── Variáveis globais ─────────────────────
   Para mudar qualquer aspecto visual do site
   inteiro, edite apenas esta seção.          */
:root {
  --bg:           #F2DFCE;
  --fg:           #000;
  --fg-text:      #3E474F;
  --fg-muted:     rgba(0,0,0,0.10);
  --accent:       #ae3321;

  --font-display: 'Playfair Display', Georgia, serif;
  --font-body:    'Lora', Georgia, serif;

  --pad-h:        36px;   /* padding horizontal do cabeçalho e áreas */
  --pad-h-mobile: 16px;

  --nav-size:     2.2rem; /* tamanho dos links do nav */
  --nav-size-mob: 1.1rem;

  --body-size:    1.15rem;
  --body-lh:      1.7;
}

/* ── Reset ─────────────────────────────── */
*, *::before, *::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body {
  height: 100%;
  overflow: hidden;
  background: var(--bg);
  color: var(--fg);
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
}

/* ── Cabeçalho ─────────────────────────── */
header {
  padding: 28px var(--pad-h) 0;
  overflow: hidden;
  position: relative;
  z-index: 20;
  display: none;
}

.titulo {
  font-family: var(--font-display);
  font-weight: 900;
  line-height: 1.1;
  white-space: nowrap;
  display: block;
  color: var(--fg-muted);
}

/* ── Nav ───────────────────────────────── */
nav {
  padding: 20px var(--pad-h) 0;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  position: relative;
  z-index: 25;
}

nav a {
  display: inline-block;
  font-family: var(--font-display);
  font-weight: 900;
  font-size: var(--nav-size);
  color: var(--fg);
  text-decoration: none;
  letter-spacing: -0.02em;
  padding: 2px 12px;
  width: fit-content;
  line-height: 1.15;
  transition: background .2s, color .2s;
  cursor: pointer;
}

nav a:hover        { background: var(--fg);     color: var(--bg); }
nav a.active       { background: var(--fg);     color: var(--bg); }
nav a.active:hover { background: var(--accent); color: var(--bg); }

nav a[href="index.html"]:hover { background: var(--accent); color: var(--bg); }

/* ── Hero decorativo ───────────────────── */
.hero-wrap {
  position: fixed;
  bottom: -0.25em;
  left: 35%;
  pointer-events: none;
  user-select: none;
  z-index: 1;
}
body.texto .hero-wrap { display: none; }
body.texto header { display: none; }
.hero-text {
  font-family: var(--font-display);
  font-weight: 900;
  line-height: 1;
  white-space: nowrap;
  letter-spacing: -0.02em;
  color: var(--fg-muted);
}

/* ── Barra de progresso de leitura ─────── */
/* Injetada dinamicamente pelo layout_master.js
   nas páginas de texto. Sem necessidade de HTML. */
#reading-bar {
  position: fixed;
  top: 0;
  left: 0;
  height: 2px;
  background: var(--accent);
  z-index: 100;
  transition: width .1s linear;
  pointer-events: none;
}

/* ── Páginas de texto ──────────────────── */
.content-area {
  position: absolute;
  left: var(--pad-h);
  right: var(--pad-h);
  bottom: -40px;
  display: flex;
  flex-direction: column;
  z-index: 15;
}

.text-title {
  font-family: var(--font-display);
  font-weight: 900;
  font-size: 1.8rem;
  letter-spacing: -0.01em;
  color: var(--fg);
  margin-bottom: 10px;
  white-space: nowrap;
  overflow: hidden;
}

.scroll-area {
  flex: 1;
  overflow: hidden;
  position: relative;
  border-top: 1px solid rgba(0,0,0,0.2);
  border-bottom: 1px solid rgba(0,0,0,0.2);
}

.scroll-inner {
  height: 100%;
  overflow-y: scroll;
  padding: 8px 0 120px;
  font-family: var(--font-body);
  font-size: var(--body-size);
  line-height: var(--body-lh);
  color: var(--fg-text);
  white-space: pre-wrap;
  tab-size: 4;
  scrollbar-width: none;
  -ms-overflow-style: none;
}

.scroll-inner::-webkit-scrollbar { display: none; }

.content-area-fade {
  position: fixed;
  left: var(--pad-h);
  right: var(--pad-h);
  height: 80px;
  background: linear-gradient(to bottom, transparent, var(--bg));
  pointer-events: none;
  z-index: 18;
}

/* ── Botão modo leitura ────────────────── */
#btn-leitura {
  position: absolute;
  top: 10px;
  right: 10px;
  width: 30px;
  height: 30px;
  background: #000;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  padding: 6px;
  z-index: 30;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: top 0.2s;
}

body.leitura #btn-leitura {
  top: 24px;
}

@media (max-width: 600px) {
  #btn-leitura {
    width: 26px;
    height: 26px;
    padding: 5px;
  }
}

/* ── Modo leitura ativo ────────────────── */
body.leitura #nav-menu          { display: none; }
body.leitura nav::before        { display: none; }
body.leitura .text-title        { display: none; }
body.leitura #reading-bar       { display: none; }

/* ── Páginas de índice ─────────────────── */
.index-area {
  position: absolute;
  left: var(--pad-h);
  right: var(--pad-h);
  bottom: 30px;
  z-index: 20;
  overflow-y: auto;
  padding-bottom: 120px;
  scrollbar-width: none;
  -ms-overflow-style: none;
}

.index-area::-webkit-scrollbar { display: none; }

.index-header     { margin-bottom: 10px; }

.index-genre-label {
  font-family: var(--font-display);
  font-weight: 900;
  font-size: 2.6rem;
  letter-spacing: -0.02em;
  color: var(--fg);
}

.index-divider {
  width: 100%;
  height: 1px;
  background: rgba(0,0,0,0.25);
  margin-bottom: 14px;
}

.index-columns {
  display: flex;
  gap: 40px;
}

.index-col {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 4px;
  flex: 1;
}

.index-col li { list-style: none; }

.index-col a {
  font-family: Optima, Candara, sans-serif;
  font-weight: 900;
  font-size: 1.375rem;
  color: #1a1a1a;
  text-decoration: none;
  line-height: 1.35;
  display: inline-block;
  padding: 1px 6px;
  transition: background .15s, color .15s;
  cursor: pointer;
  position: relative;
  z-index: 50;
}

.index-col a:hover {
  background: var(--accent);
  color: var(--bg);
}

nav::before {
    content: '© Luiz Fonte Boa';
    font-family: 'Calibri', sans-serif;
    font-weight: 900;
    font-size: 1.3rem;
    color: #fff;
    padding: 2px 12px;
    display: block;
    text-align: right;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    background: var(--accent);
    margin-bottom: 12px;
  }

/* ── Mobile ────────────────────────────── */
@media (max-width: 600px) {
  nav {
    margin-top: 20px;
    padding: 0 var(--pad-h-mobile) 0;
    align-items: flex-end;
  }
  nav a { font-size: var(--nav-size-mob); padding: 1px 6px; }
  header { padding: 20px var(--pad-h-mobile) 0; }
  .content-area,
  .index-area          { left: var(--pad-h-mobile); right: var(--pad-h-mobile); }
  .content-area-fade   { left: var(--pad-h-mobile); right: var(--pad-h-mobile); }
  nav::before {
    font-size: 0.75rem;
    padding: 1px 6px;
  }
  .text-title {
    font-size: 1.1rem;
  }
  .index-col a {
    font-size: 1rem;
  }
}

/* ── Widescreen ────────────────────────── */
@media (min-width: 900px) {
  nav {
    padding-top: 20px;
    align-items: flex-end;
    flex-shrink: 0;
  }
  nav a {
    font-size: 1.4rem;
  }
}

body {
    animation: fade-in 2s ease;
}

@keyframes fade-in {
    from { opacity: 0; }
    to   { opacity: 1; }
}

/* ── Modal ── */
#modal-overlay {
  display: none;
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.4);
  z-index: 500;
  align-items: center;
  justify-content: center;
}
#modal-overlay.visivel { display: flex; }
#modal-box {
  width: 400px;
  max-width: 90vw;
  background: rgba(242, 223, 206, 0.95);
  box-shadow: 0 8px 32px rgba(0,0,0,0.22);
  display: flex;
  flex-direction: column;
}
#modal-titulo {
  background: #000;
  color: #fff;
  font-family: Georgia, serif;
  font-weight: 900;
  font-size: 0.78rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  text-align: center;
  padding: 10px 36px;
  user-select: none;
  position: relative;
}
#modal-fechar {
  position: absolute;
  right: 12px;
  top: 50%;
  transform: translateY(-50%);
  background: none;
  border: none;
  color: #fff;
  font-size: 1rem;
  cursor: pointer;
  opacity: 0.7;
  transition: opacity 0.15s;
}
#modal-fechar:hover { opacity: 1; }
#modal-corpo {
  padding: 18px 20px 20px;
  font-family: Georgia, serif;
  font-size: 0.85rem;
  color: #1a1a1a;
  line-height: 1.3;
  text-align: center;
}
#modal-contato {
  margin-top: 14px;
  background: #000;
  color: #fff;
  font-family: Georgia, serif;
  font-weight: 900;
  font-size: 0.58rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  border: none;
  padding: 6px 11px;
  cursor: pointer;
  transition: background 0.2s;
  display: block;
  margin-left: auto;
  margin-right: auto;
}
#modal-contato:hover { background: #ae3321; }
#modal-email {
  margin-top: 10px;
  font-family: Georgia, serif;
  font-size: 0.82rem;
  color: #1a1a1a;
  display: none;
}